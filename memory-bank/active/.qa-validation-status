PASS

Semantic review of interactive-prompt-backspace against the Level 2 plan and projectbrief. Implementation is acceptable as-is.

Covered: `edit_prompt_line` / `read_prompt_line` in `ai-rizz` Utilities; four `cmd_init` / `cmd_deinit` call sites; `tests/unit/test_prompt_line_edits.test.sh`; `test_init_mode_prompt_backspace_accepts_local`. Remaining `read -r` uses are file/pipe loops, as scoped.

Advisories (do not block):
- The INT/TERM/EXIT trap restores `stty` and does not `exit`. On dash and bash 5, a process-group SIGINT (Ctrl+C analog) still ends the script with 130 after the trap, so empty-mode → local does not run. An explicit `exit 130` would make that contract obvious on unknown shells; not required.
- Header says printable bytes append; the loop appends any byte that is not NL/CR/BS/DEL. Fine for the ASCII prompts in scope.
- On a cbreak tty, Ctrl+D is EOT (`\004`), not a 0-byte `dd` read. Pipe/file EOF is handled. Enter still submits.
- `systemPatterns.md` does not yet say interactive stdin prompts must use `read_prompt_line`. Omission is allowed by that file’s inclusion bar; Reflect may add a one-liner if wanted.
