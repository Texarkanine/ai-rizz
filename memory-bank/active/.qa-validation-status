FAIL

Task: skill-support
Date: 2026-02-20
Phase: QA (second pass)

Blocking finding:
- cmd_deinit() does not remove .cursor/skills/<mode>/ directories when deiniting a mode.
  Local deinit should rm -rf ".cursor/skills/${LOCAL_DIR}" (alongside L3082)
  Commit deinit should rm -rf ".cursor/skills/${SHARED_DIR}" (alongside L3100)
  Global deinit should rm -rf "${GLOBAL_SKILLS_DIR}" (alongside L3115)
  Tests needed: new test cases for skills dir removal on deinit for each mode.

Next step: /build
