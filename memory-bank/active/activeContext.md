# Active Context

**Task ID:** ruleset-skills-cmd-routing
**Phase:** QA COMPLETE (verification after reflect)
**Focus:** ruleset `skills/**/*.md` excluded from flat command deploy; regression covered by `test_md_under_skills_dir_not_deployed_as_flat_commands`.

**Next:** Operator may run `/niko-archive` to archive this standalone task when ready.
